specVersion: "v1beta"
serviceId: "parchez"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "parchez-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
